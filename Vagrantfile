# -*- mode: ruby -*-
# vi: set ft=ruby :
Vagrant.configure("2") do |config|
  config.vm.box = "Anton-Terekhov/centos-7-8"
  config.vm.box_version = "1.0"

config.vm.synced_folder "/root/HW1/01/share", "/share"
end

